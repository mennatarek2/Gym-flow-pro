# Leave empty to use Vite proxy (/api → VITE_API_PROXY_TARGET).
VITE_API_BASE_URL=

# Match the Swagger origin:
#   https://localhost:5001  ← GMS.Api "https" profile (typical)
#   http://localhost:5000   ← same profile’s HTTP port
VITE_API_PROXY_TARGET=https://localhost:5001
